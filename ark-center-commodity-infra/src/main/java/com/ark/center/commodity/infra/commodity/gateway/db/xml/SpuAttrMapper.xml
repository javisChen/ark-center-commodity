<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ark.center.commodity.infra.commodity.gateway.db.SpuAttrMapper">

    <select id="selectBySpuId" resultType="com.ark.center.commodity.client.commodity.dto.AttrDTO">
        select cpa.attr_id, cpa.attr_value, ca.name as attr_name
        from co_spu_attr cpa
        inner join co_attr ca on cpa.attr_id = ca.id
        where cpa.spu_id = #{spuId}
    </select>
</mapper>
