<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ark.center.commodity.infra.commodity.gateway.db.SpuAttrMapper">

    <select id="selectSpuSpecs" resultType="com.ark.center.commodity.client.commodity.dto.AttrDTO">

        select ca.id as attr_id,
               ca.name as attr_name
        from co_attr ca
                 inner join co_attr_template cat on ca.attr_template_id = cat.id
                 inner join co_category cc on cc.attr_template_id = cat.id
                 inner join co_spu cs on cs.category_id = cc.id
        where cs.id = #{spuId} and ca.type = 1

    </select>
</mapper>
